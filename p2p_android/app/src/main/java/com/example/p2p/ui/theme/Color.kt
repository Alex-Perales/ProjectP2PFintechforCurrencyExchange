package com.example.p2p.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Principal ───────────────────────────────────────────────────────────
val Primary      = Color(0xFF0097A7)   // Teal profundo — confianza y solidez
val PrimaryLight = Color(0xFF00BCD4)   // Teal medio
val PrimaryDark  = Color(0xFF006978)   // Teal oscuro — hover / pressed
val PrimaryMint  = Color(0xFF00C897)   // Acento verde-menta — éxito / confirmación

// ── Fondos ────────────────────────────────────────────────────────────────────
val BackgroundApp     = Color(0xFFF0F4F8)   // Fondo global — azul-gris muy sutil
val SurfaceColor      = Color(0xFFFFFFFF)   // Tarjetas y superficies
val SurfaceElevated   = Color(0xFFF8FAFC)   // Superficies ligeramente elevadas
val DarkSurface       = Color(0xFF0F172A)   // Tarjetas oscuras (headers)
val DarkSurface2      = Color(0xFF1E293B)   // Secundario oscuro

// ── Estado ────────────────────────────────────────────────────────────────────
val SuccessColor = Color(0xFF059669)   // Esmeralda — completado
val DangerColor  = Color(0xFFDC2626)   // Rojo limpio — error / cancelado
val WarningColor = Color(0xFFD97706)   // Ámbar — pendiente / advertencia
val InfoColor    = Color(0xFF3B82F6)   // Azul — información

// ── Texto ─────────────────────────────────────────────────────────────────────
val TextMain    = Color(0xFF0F172A)   // Slate 900 — máximo contraste
val TextSecond  = Color(0xFF334155)   // Slate 700 — texto secundario
val TextMuted   = Color(0xFF64748B)   // Slate 500 — texto suavizado
val TextSubtle  = Color(0xFF94A3B8)   // Slate 400 — placeholders

// ── Bordes y divisores ────────────────────────────────────────────────────────
val BorderColor  = Color(0xFFE2E8F0)   // Slate 200 — bordes de tarjetas
val DividerColor = Color(0xFFF1F5F9)   // Slate 100 — divisores internos

// ── Overlay ───────────────────────────────────────────────────────────────────
val OverlayLight = Color(0x0F000000)   // 6% negro — sombreado suave
val OverlayDark  = Color(0x52000000)   // 32% negro — modal / scrim

// ── Bancos peruanos ───────────────────────────────────────────────────────────
val BcpColor       = Color(0xFFE85D04)   // BCP naranja
val InterbankColor = Color(0xFF00873A)   // Interbank verde
val BbvaColor      = Color(0xFF004481)   // BBVA azul marino
val YapeColor      = Color(0xFF6B21A8)   // Yape morado
val PlinColor      = Color(0xFF0891B2)   // Plin cyan
